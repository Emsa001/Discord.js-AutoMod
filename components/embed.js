const config = require('../config.json');

const footer = {
    text: 'NewGenerationServers by Emsa001#7224 & i<ala#0001',
    iconURL: config.icons.newGenerationServers,
};
module.exports = { footer };
